databases:
  - name: courtly-db
    databaseName: courtly_zriz
    user: courtly_user
    plan: free
    region: ohio

services:
  - type: web
    name: courtly-api
    runtime: docker
    plan: free
    region: ohio
    branch: main
    dockerfilePath: Dockerfile
    healthCheckPath: /health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: DB_URL
        fromDatabase:
          name: courtly-db
          property: internalConnectionString
      - key: DB_USERNAME
        fromDatabase:
          name: courtly-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: courtly-db
          property: password
      - key: AUTHORIZATION_JWT_SECRET
        value: courtly-super-secret-jwt-key-2026
      - key: AUTHORIZATION_JWT_EXPIRATION_DAYS
        value: 7